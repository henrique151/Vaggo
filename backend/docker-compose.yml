services:
  api:
    build: .
    container_name: vaggo_api
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    command: sh -c "npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all && npm start"